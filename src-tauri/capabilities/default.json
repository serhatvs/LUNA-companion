{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Luna's overlay window talks to the core event bus and her own commands only.",
  "windows": ["luna"],
  "permissions": ["core:default"]
}
