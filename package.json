{
  "name": "luna-companion",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "description": "A featherweight desktop cat that waits for your builds",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "cli:pack": "node scripts/pack-cli.mjs",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.0",
    "typescript": "~5.9.3",
    "vite": "^7.2.6"
  }
}
